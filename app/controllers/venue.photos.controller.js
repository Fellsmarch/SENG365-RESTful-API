const UserPhotos = require("../models/user.photos.model");
const Auth = require("../util/util.authorization");
const fs = require("fs");

exports.addVenuePhoto = function(req, resp) {
    resp.send();
};

exports.getVenuePhoto = function(res, resp) {
    resp.send();
};

exports.deleteVenuePhoto = function(res, resp) {
    resp.send();
};

exports.setPrimaryPhoto = function(res, resp) {
    resp.send();
};
